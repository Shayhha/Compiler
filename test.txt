public int hi(args>> int: a,b,c,d; bool: x): static {
    var int: var1, var2, var3 <- 45;
    var bool: m <- true, n <- (((a < 3) || !(a == 8)));
    var int*: ptr;
    string message[20] <- "asd";
    var char: pp;

    if (a > b){
        a <- a + 7;
    } 
    else {
        c <- 1 + 2;
    }

    if ( (c > d) && !('a' != 'v') && (pp != message[2]) )
        d <- a + d;

    return 123;
}

public void main(): static{
    var int: a;

    for(var int: i <- 0 ; i < 10 ; i <- i + 1 ) {
        a <- a + i;
    
        while (a != 5)
            a <- a + 1;

        a <- 7;
        do {
            a <- a + i;
            i <- i + 1;
        } while(hi(1, 2, 3, 4, true) >= 10);
    }
}